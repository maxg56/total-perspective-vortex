[project]
name = "total-perspective-vortex"
version = "0.1.0"
description = "EEG Brain-Computer Interface for motor imagery classification"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "requests>=2.32.5",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
